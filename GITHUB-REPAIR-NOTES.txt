Dr.Ahmad — repair for the 174/9 regression

Root cause:
The polish commit replaced complete versions of ArticleReader.tsx, ArticleDetail.tsx and index.css
with older/smaller snapshots. That removed existing reader-selection, central glossary, multi-signal
article markers, clean selection toolbar, anti-gloss rules and print contracts.

The repair script:
- restores only those protected reader/style layers from commit 6265d529...
- keeps the new Information Architecture in ui.tsx
- removes only the unwanted numeric group count from the menu
- keeps the current constellation glossary sync in main.tsx
- places the genuinely new polish in a small CSS override loaded after the canonical stylesheet
- runs critical/visual/architecture/tanween/Arabic typography/all-user-notes/site-polish/TypeScript checks

Run from the repository root:
  bash apply-github-regression-fix.sh

Then inspect:
  git diff

If green:
  git add src scripts/test-site-polish-2026.mjs
  git commit -m "fix: restore protected reader layers after site polish"
  git push
