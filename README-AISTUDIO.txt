========================================================
 تنظيف مشروع AI Studio — إزالة لعبة العائلة نهائياً
========================================================

الهدف: يصير مشروعك في AI Studio أكاديميّاً نظيفاً، فتتوقف
اللعبة عن الظهور في كل دفعة، ويتوقف الصراع للأبد.

------------------------------------------------
الخطوة ١) احذف ملفات اللعبة من AI Studio (إن وُجدت):
------------------------------------------------
  - src/components/MotionGame.tsx
  - src/galaxyEngine.ts    (أو galaxyEngine.tsx)
  - src/galaxyAudio.ts     (أو galaxyAudio.tsx)
  - server.js
  - UPDATE_AISTUDIO.md

------------------------------------------------
الخطوة ٢) ارفع الملفات النظيفة المرفقة (بنفس مساراتها):
------------------------------------------------
  - src/App.tsx                          ← الموقع الأكاديمي (يستبدل اللعبة)
  - src/index.css                        ← نظيف بلا أنماط لعبة
  - src/data.ts                          ← تحديث اليوم (روابط Scholar/RG)
  - src/pages/ArticleDetail.tsx          ← تحديث اليوم (شريط التحديد + استشهاد)
  - src/pages/PaperDetail.tsx            ← تحديث اليوم (أيقونات + استشهاد)
  - src/components/IdeaFeatures.tsx      ← أداة التحديد الموحّدة
  - src/components/admin/VoiceBakeoff.tsx

------------------------------------------------
الخطوة ٣) اضغط Publish.
------------------------------------------------
بعدها: مساحتك بلا لعبة  ->  دفعتك نظيفة  ->  الحارس يمرّرها
       ->  GitHub يصير نظيفاً تلقائياً  ->  لا صراع أبداً.

ملاحظة: الحارس (guard-game.yml) يبقى كدرعٍ صامت؛ لن يتدخّل
        إلا لو عادت اللعبة يوماً. لم يعد خطراً عليك.
