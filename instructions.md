# قواعد تحرير الموقع الدائمة

## التنوين العربي
في **كل النصوص العربية بالموقع، ولوحة التحكم، والمحتوى القادم من Firestore** تُعتمد القاعدة التالية دائماً:

- تنوين الفتح على ألف النصب: **«مشروعاً»**، وتوضع علامة التنوين على الألف نفسها لا قبلها.
- تنوين الضم ملاصق للحرف: **«ممدوحٌ»**.
- تنوين الكسر ملاصق للحرف: **«كتابٍ»**.
- لا تُفصل علامة التنوين عن الحرف بمسافة، ولا تُكرر العلامة.

هذه ليست محاولة لتخمين الإعراب آلياً؛ النظام يحافظ على التنوين الذي كتبه المؤلف ويصحح **موضعه الطباعي** فقط. القاعدة مطبقة عند الحفظ والعرض والبناء الثابت، ويحرسها `scripts/guard-arabic-tanween.mjs` داخل CI حتى لا ترجع الصيغة القديمة في تحديث مستقبلي.

## قاعدة الصوت — نورة وفهد
- **صفحة المقال العامة فقط** تستخدم الاسم المحايد **«قراءة المقال»** ولا تعرض اسم نورة أو فهد للزائر داخل المشغّل.
- في **قافلة الصوت، مكتبة الصوت، أدوات الإدارة، التقارير وواتساب** تظهر المسارات الحقيقية بوضوح: **«صوت نورة» · «صوت فهد» · «الحوار»**.
- أي تطوير لاحق لا يجوز أن يحوّل إخفاء الاسم في المقال إلى إخفاء شامل عن لوحات الإنتاج.
- المقال المنشور الجديد يدخل تلقائياً مساري نورة وفهد، ويدخل مسار الحوار. الحوار الآلي لا يُنشأ من خارج نص المقال، وأثر الفكرة الخارجي لا يُعرض إلا بدليل قابل للتحقق.

## مكتبة التوليد في استوديو التصاميم
- «مكتبة التوليد» عنصر دائم في الاستوديو، ويجب أن تُرندر **خارج** فرعي `SIMPLIFIED_STUDIO` حتى تبقى متاحة قبل كتابة فكرة جديدة.
- الحفظ الدائم يعتمد Firebase Storage + Firestore، لذلك فشل نشر `storage.rules` يُعد فشل نشر للميزة ولا يجوز تمريره كنجاح صامت.
- فحص الجاهزية الحقيقي يرفع ملفاً صغيراً ويقرأه ثم يحذفه بصلاحية المشرف.
- استعادة التاريخ القديم (Backfill) تكون فقط من مخرجات يمكن إثباتها، مثل التصاميم المحفوظة محلياً والأصول المضمّنة فيها؛ لا تُنشأ سجلات تاريخية بالتخمين.
