# Security Notes — مساعد واتساب

- جلسة Baileys لا تُحفظ في Git أو Firebase أو ZIP.
- مفاتيح الجلسة في SQLite محلي مشفّر بـ AES-256-GCM، والمفتاح في macOS Keychain.
- QR ورمز الاقتران لا يُحفظان في SQLite ولا يعبران جسر المتصفح؛ يظهران مؤقتًا في جلسة الطرفية فقط.
- لا تُطبع JIDs أو أرقام الهاتف كاملة في السجلات؛ تُستخدم بصمات opaque أو إخفاء جزئي.
- أسماء وأرقام جهات الاتصال، والتذكيرات، ومقتطفات الطلبات غير المفهومة تُشفّر عند التخزين المحلي.
- لا تحفظ نصوص المحادثات كاملة افتراضيًا، وتُنظّف السجلات بعد 30 يومًا.
- الإرسال معطّل افتراضيًا، والرد الآلي معطّل افتراضيًا.
- لا يمرر نص واتساب إلى Shell ولا يفتح الروابط الواردة تلقائيًا.
- لا يتم تعديل قوائم البث الأصلية أو إضافة أعضاء إليها.
- `logout` يحتاج تأكيدين ويحذف الجلسة المحلية فقط.
- لا يوجد ضمان بعدم حظر رقم واتساب؛ الربط غير رسمي، لذلك يبدأ الاختبار بالذات ثم شخص واحد فقط.
- أي ربط من لوحة الموقع ببيانات الوكيل يجب أن يكون عبر جسر محلي مصادق، وليس endpoint عامًا.
