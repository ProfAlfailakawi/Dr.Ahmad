# DESIGN.md — د. أحمد حسين الفيلكاوي

> نظام التصميم البصري للموقع، مكتوباً ليقرأه إنسانٌ ووكيلٌ ذكيّ معاً.
> **كل قيمةٍ هنا مقيسةٌ من الشيفرة نفسها** (`src/index.css` و`tailwind.config.js`)،
> لا مقترحةً ولا مؤلَّفة. إن اختلف هذا الملف عن الشيفرة فالشيفرة هي الحق،
> ويُصحَّح هذا الملف — لا العكس.
>
> الصيغة على مواصفة `DESIGN.md` (Google Stitch)، وهي المواصفة التي يجمعها
> مستودع `voltagent/awesome-design-md`.

---

## ١) الجوّ البصري والفلسفة

موقعُ أستاذٍ جامعيّ يكتب بالعربية، لا لوحةُ منتَجٍ رقميّ. الهوية **ورقيّة
هادئة**: قماشةٌ عاجية دافئة لا بيضاء صريحة، وحبرٌ رماديّ داكن لا أسود قاسٍ،
ولمسةُ أزرقٍ رصينة واحدة. لا تدرّجات، ولا ألوان صارخة، ولا ظلالٌ ثقيلة.

المبادئ الحاكمة:

- **الفراغ عنصرٌ لا فراغ.** المساحة البيضاء هي ما يجعل النصّ العربي يتنفّس.
- **خطٌّ شعريّ للعناوين، خطٌّ صافٍ للمتن.** التباين بين العائلتين هو كلّ الزينة.
- **حدٌّ شعريّ (hair) بدل الظلّ.** الفصل بخطٍّ رفيع أولاً، والظلّ استثناء.
- **الحركة تُروي ولا تُلفت.** الانتقالات بطيئة ومنحنى واحد لكل الموقع.
- **RTL أصلٌ لا ترجمة.** التخطيط يُبنى من اليمين ابتداءً.

---

## ٢) لوحة الألوان وأدوارها

المتغيّرات مخزّنةٌ قنواتٍ RGB (بلا `rgb()`) كي تعمل معها شفافية Tailwind
(`bg-accent/[.05]`). **لا تكتب لوناً حرفياً في مكوّن؛ استعمل الرمز.**

### النهار (`:root`)

| الرمز | القيمة | HEX | الدور |
|---|---|---|---|
| `--c-canvas` | `252 252 250` | `#FCFCFA` | أرضية الصفحة — عاجيّ دافئ لا أبيض |
| `--c-ink` | `21 22 26` | `#15161A` | النصّ الأساسي والعناوين |
| `--c-soft` | `94 101 112` | `#5E6570` | النصّ الثانوي والبيانات الوصفية |
| `--c-accent` | `62 92 120` | `#3E5C78` | الروابط والأفعال والتمييز |
| `--c-accent-deep` | `51 80 107` | `#33506B` | حالة التمرير/الضغط للأفعال |
| `--c-wash` | `244 245 243` | `#F4F5F3` | سطحٌ مرتفع: البطاقات والحقول |
| `--c-hair` | `rgba(21,22,26,.09)` | — | الحدّ الشعريّ الفاصل |

### الليل (`html.dark`)

| الرمز | القيمة | الدور |
|---|---|---|
| `--c-canvas` | `15 17 21` | مساءٌ أعمق وأدفأ من الأسود الصريح |
| `--c-ink` | `236 234 228` | عاجيّ خافت بدل الأبيض الحادّ |
| `--c-soft` | `181 184 190` | ثانويّ مقروء على العتمة |
| `--c-accent` | `132 169 202` | أزرق مسائيّ (يفتحّ لا يغمق) |
| `--c-accent-deep` | `164 194 220` | تمرير — **أفتح** من الأساس عكس النهار |
| `--c-wash` | `23 26 31` | سطحٌ داكن بطبقةٍ مختلفة |
| `--c-hair` | `rgba(234,234,231,.11)` | حدٌّ شعريّ على العتمة |

**قاعدة قاطعة:** الليل ليس عكساً حسابياً للنهار. اللهجة تدفأ والتباين يخفّ.

---

## ٣) قواعد الطباعة

| الاستعمال | العائلة | الصنف | الوزن |
|---|---|---|---|
| العناوين | `"El Messiri", serif` | `font-display` | 600 |
| المتن والواجهة | `"Tajawal", system-ui` | `font-sans` (افتراضي) | 300–500 |

**المِطبّ المرصود:** المتاح محلياً من Tajawal ينتهي عند الوزن **500**. لذلك
`.font-semibold` و`.font-bold` تُخفَّض قسراً إلى 500 خارج `font-display`، منعاً
للتغليظ الاصطناعي (المتصفّح يزيّف الثقل فيشوّه الحرف العربي).

قواعد أخرى:

- المقاسات كسريّة مقصودة (`text-[.84rem]`, `text-[1.18rem]`) — لا سلّم مستدير.
- `leading-relaxed` هو الأصل للنصّ العربي؛ الأسطر المتقاربة تخنقه.
- **أرقام لاتينية دائماً** (١٢٣ ← 123) في كلّ ما يُعرض للزائر.
- «تكنولوجيا» لا «تقنية» — لفظ الموقع المعتمد في كل نصٍّ يُكتب.
- «صيدة/صيد» ممنوعة في نصوص الواجهة (غير لائقة في الكويت) — البديل: «مادة جديدة».

---

## ٤) تنسيق المكوّنات

**الزرّ الأساسي:** خلفية `accent`، نصّ `canvas`، زوايا 12px، ارتفاع مريح للمس.
عند التمرير ينتقل إلى `accent-deep` بمنحنى `--E`.

**الزرّ الشبحي:** بلا خلفية، حدّ `hair`، نصّ `ink`؛ يكتسب `wash` عند التمرير.

**البطاقة:** خلفية `wash`، حدّ `hair`، زوايا 12px، **بلا ظلّ افتراضاً**.
الظلّ (`--shadow-soft`) للطبقات العائمة وحدها: النوافذ والقوائم المنسدلة.

**الحقل:** خلفية `canvas`، حدّ `hair`؛ عند التركيز يصير الحدّ `accent`.

**الشريط الأفقي (rail):** التمرير يمين/يسار مطلوبٌ ومقصود — هو جمالُ الموقع.
الممنوع «النقزة» (القفزة المفاجئة) وحدها. `--rail-fade: 40px` يذيب الحافّة.

---

## ٥) مبادئ التخطيط

- **عرض القشرة:** `max-w-shell` = **1140px**، ثم `mx-auto`.
- **الحواف:** `px-6` على الهاتف، `md:px-11` على الشاشات الأوسع.
- **نصف القطر موحّد 12px** لكلّ الأحجام (`xl`/`2xl`/`3xl`) — قرارٌ مقصود:
  لا تسلسل زوايا في هذا الموقع.
- الشبكات تنهار إلى عمودٍ واحد على الهاتف دائماً.

---

## ٦) العمق والارتفاع

| الرمز | القيمة | الاستعمال |
|---|---|---|
| `--shadow-soft` | `0 22px 44px -26px rgba(21,22,26,.4)` | النوافذ والطبقات العائمة |
| `--shadow-deep` | `0 36px 64px -36px rgba(21,22,26,.42)` | أعلى طبقة وحدها |

الظلال **منتشرة وخافتة وموجبة الإزاحة رأسياً** — ضوءٌ من أعلى، لا هالة.
في الليل تعتمد على أسود صريح بشفافية أعلى (.75/.8) لأن الأرضية أصلاً داكنة.

**سلّم الارتفاع:** `canvas` → `wash` → حدّ `hair` → ظلّ. لا تقفز إلى الظلّ
قبل أن تستنفد ما قبله.

---

## ٧) افعل ولا تفعل

**افعل**

- استعمل الرموز الدلالية (`bg-canvas`, `text-soft`, `border-hair`).
- افصل بالحدّ الشعريّ أولاً.
- اجعل الحركة بمنحنى `--E` = `cubic-bezier(.2,.7,.2,1)` وحده.
- اختبر كلّ شاشةٍ في الليل والنهار — الوضعان مواطنان متساويان.

**لا تفعل**

- ❌ **لا شفافية على `hair` إطلاقاً** (`border-hair/50`). القيمة `rgba()` جاهزة
  لا قنواتٌ RGB، فيسقط المُعدِّل **صامتاً** ويختفي الحدّ. حارسٌ آليّ يمنعها.
- ❌ لا `overflow-x: hidden` في واجهات الزوار — يقتل `position: sticky`.
  البديل `overflow-x: clip`.
- ❌ لا تكتب لوناً حرفياً (`#3E5C78`) في مكوّن.
- ❌ لا تُغلّظ Tajawal فوق 500.
- ❌ لا تدرّجات ولا ألوان مشبعة ولا ظلال ملوّنة.
- ❌ **لا تغيّر جمالياتٍ قائمة بلا طلبٍ صريح من الدكتور.**

---

## ٨) السلوك التجاوبيّ

- نقطة الانكسار العاملة الوحيدة عملياً هي `md` (768px).
- الأشرطة اللاصقة: داخل التدفّق على الهاتف، `md:sticky md:top-16` فوقها —
  لأن اللصق على الهاتف يأكل شاشةً صغيرة أصلاً.
- أهداف اللمس ≥ 44px.
- كلّ حاويةٍ عريضة (جدول، رسم، شيفرة) تُمرَّر داخل `overflow-x: auto` بنفسها؛
  **جسم الصفحة لا يُمرَّر أفقياً أبداً**.

---

## ٩) دليل الوكيل الذكيّ

انسخ الكتلة التالية إلى أيّ وكيلٍ يولّد واجهةً لهذا الموقع:

```
الهوية: موقع أكاديميّ عربيّ RTL، جوّ ورقيّ هادئ.
الأرضية #FCFCFA · الحبر #15161A · الثانوي #5E6570 · التمييز #3E5C78
السطح المرتفع #F4F5F3 · الحدّ rgba(21,22,26,.09)
الليل: أرضية #0F1115 · حبر #ECEAE4 · تمييز #84A9CA
العناوين "El Messiri" 600 · المتن "Tajawal" 300–500 (لا تتجاوز 500)
نصف القطر 12px لكل شيء · العرض الأقصى 1140px · الحواف 24px / 44px
الفصل بحدٍّ شعريّ لا بظلّ · منحنى الحركة cubic-bezier(.2,.7,.2,1)
أرقام لاتينية · «تكنولوجيا» لا «تقنية» · بلا تدرّجات ولا ألوان صارخة
```

**أسئلة يجب أن يجيب عنها الوكيل بنعم قبل التسليم:**

1. هل تعمل الشاشة في الليل والنهار بلا لونٍ محفور؟
2. هل كلّ لونٍ جاء من رمزٍ دلاليّ؟
3. هل جسم الصفحة خالٍ من التمرير الأفقي على 375px؟
4. هل استُعمل الحدّ الشعريّ قبل الظلّ؟
5. هل بقيت جماليات الدكتور القائمة كما هي؟
