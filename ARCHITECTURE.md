# معمارية الموقع ولوحة التحكم

هذا الملف يحفظ ترتيب النظام بعد المراجعة المعمارية، من دون حذف أي ميزة أو تغيير محتواها.
الأرقام الحيّة في [PROJECT-STATUS.md](PROJECT-STATUS.md) — مولَّدةً لا مكتوبة.

## الطبقات

| الطبقة | الموضع | ملاحظة |
| --- | --- | --- |
| العرض | `src/pages` · `src/components` | React 19 · Tailwind · Framer Motion |
| التوجيه | `src/App.tsx` | React Router 8؛ جدول المسارات مصدرٌ واحد |
| البيانات الثابتة | `src/data.ts` · `src/data/**` | الأساس؛ الموقع يعمل بها وحدها |
| Firestore | [docs/FIRESTORE.md](docs/FIRESTORE.md) | طبقة إثراءٍ وتحرير، لا شرطَ تشغيل |
| التخزين | Firebase Storage · R2 للصوت | الصوت خارج المستودع دائماً |
| المصادقة | Firebase Auth + ادّعاء `admin` | لا حسابات زوّار |
| الخادم | `server.mjs` على Cloud Run | نداءات الإدارة والتحليلات والإرسال |
| البناء والحرّاس | `scripts/**` | تُفصَّل في README |

## الأنظمة

| النظام | المدخل | الوثيقة |
| --- | --- | --- |
| الموسوعة والبحث | `EncyclopediaPortal` · `encyclopedia-*` | [docs/SEARCH.md](docs/SEARCH.md) |
| الصوت | `auto-audio.mjs` · `audio-supervisor.mjs` | [docs/MEDIA-AUDIO-HOSTING.md](docs/MEDIA-AUDIO-HOSTING.md) |
| البودكاست | `podcast-dialogue.mjs` · `ManualDialogueEditor` | [docs/PODCAST.md](docs/PODCAST.md) |
| الاستشهادات | `src/lib/bibtex.ts` | [docs/CITATIONS.md](docs/CITATIONS.md) |
| التحليلات | `usage-analytics.ts` · `admin-usage*.ts` | [docs/ANALYTICS.md](docs/ANALYTICS.md) |
| أنظمة التحرير | `editorial-*` · `PublishingStudio` | مجلس التحرير والذاكرة والاستشراف |
| النشر | ثلاث طبقاتٍ مستقلّة | [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md) |

## الحدود الأمنية

- كل نداءٍ إداريّ يتحقّق من رمز Firebase ومن ادّعاء `admin` قبل أي عمل.
- `/admin` بـ`noindex, nofollow`، وتحليلاتها لا تظهر في الموقع العام.
- لا سرّ في المستودع؛ المستودع عامّ عمداً، والعلاج إخراجُ الملف لا خصخصةُ المستودع.
- عبارات البحث تُنقّى وتُقصّ، ويُحجب ما يشبه بريداً أو رقم هاتف قبل التسجيل.
- الصوت المولَّد لا يُلتزم في git إطلاقاً (`guard-no-generated-audio-in-git`).

## استراتيجيات السقوط (fallback)

- تعذُّر Firebase لا يوقف التصفّح — البيانات الثابتة تكفي.
- تعطُّل التحليلات لا يعطّل صفحةً ولا زرّاً؛ الفشل يُبتلع صامتاً.
- غياب تفريغٍ زمنيّ لا يُنتج توقيتاً مختلقاً — يُعرض الفيديو من بدايته.
- فشل مصدرٍ في بحث الموسوعة لا يمنع بقيّة المصادر.
- تعذُّر خدمةٍ خارجية للتعريب يهبط إلى مسارٍ مجانيّ لا يتوقّف.
- استئناف إنتاج البودكاست يعيد المفقود وحده، لا كل المقاطع من أولها.

## الموقع العام

### الاكتشاف
- الرئيسية
- البحث في المعرفة: `/search` للمادة المعروفة و`/ask` للسؤال الموثق

### القراءة
- المقالات، الكتب، الأبحاث، الإعلام
- تفاصيل كل مادة تحتفظ بأدواتها الأصلية

### منظومة الفكرة
- الباب الجامع: `/thought`
- سماء المقالات: `/atlas`
- مسارات الفكرة: `/thought-paths`
- وثيقة العقد: `/decade`
- سجل الأثر: `/impact`

### الانتقاء والتفاعل
- المختارات، السؤال، الرادار، رسائل الهامش

### الهوية والتواصل
- السيرة، اللقاءات، التواصل العربي والإنجليزي

## لوحة التحكم

`src/components/admin/admin-navigation.ts` هو المصدر الوحيد لتعريف المجالات والشاشات والروابط العميقة.

### النظام
- غرفة القيادة: القرارات المطلوبة الآن
- مراقب الإنتاج: حالة المنظومة
- صحة المحتوى: اكتمال الأرشيف والمصادر
- المختبر: التجارب وتطوير الفكرة

### الإنتاج والنشر
1. الإنشاء: النشر، التصاميم، الصور
2. الصوت: القافلة، الإنتاج، الحوار، المكتبة
3. إعدادات الصوت: النطق والأصوات
4. الإطلاق

### المكتبة
المقالات، الكتب، الأبحاث، الإعلام، اللقاءات، السيرة.

### الجمهور
الرسائل، مساعد واتساب، رسائل البوت، التحليلات ورحلة الزائر.

## حدود الشفرة

- `src/`: المصدر الحي الوحيد للواجهة.
- `src/components/admin/`: وحدات لوحة التحكم؛ لا تُضاف قائمة تبويبات خارج `admin-navigation.ts`.
- `public/`: ملفات ثابتة تُنسخ كما هي.
- `scripts/`: البناء والفحوص والأتمتة فقط.
- `dist/`: ناتج بناء؛ لا يُعدّل يدوياً.
- أي ملفات قديمة في الجذر تُعامل كتراث انتقال وتزيلها أداة التنظيف قبل البناء.

## قواعد عدم الكسر

- لا تُحذف ميزة لتقليل الزحام؛ تُنقل تحت باب أو مرحلة أو كشف تدريجي.
- لا تُكرّر أداة إنتاج في المختبر أو غرفة القيادة؛ يوضع رابط إلى الأداة الأصلية.
- أي شاشة إدارية جديدة تُسجّل مرة واحدة في `admin-navigation.ts` ثم تُضاف إلى خريطة العرض في `Admin.tsx`.
- أي نافذة عامة يجب أن تدعم Escape، حبس التركيز، وإعادة التركيز إلى زر الفتح.
- صفحات الإنجليزية يجب أن تملك نهاية رحلة إنجليزية، لا أن تعيد الزائر إلى نموذج عربي.

## إضافات القياس والبحث والإنتاج — أغسطس 2026

- القياس العام يمر عبر `/api/analytics/event` إلى `analytics_events` المجهولة، وتعرضه لوحة التحليلات الحالية فقط للأدمن.
- قياس الأدوات الإدارية دلالي في `admin_tool_events` ويمكن تعطيله، ولا يسجل النصوص التحريرية الكاملة.
- Core Web Vitals تجمع من المستخدم الحقيقي عبر PerformanceObserver وتعرض p75 وحجم العينة.
- بحث الموسوعة يحفظ الحالة في الرابط ويمنع التشغيل الصوتي التلقائي عند الاستعادة.
- الاستشهادات تستخدم مولد BibTeX المشترك بدل إنشاء أداة مستقلة.
- البودكاست يبقى يدوي الحوار، ويثبت التطابق قبل النشر، ويعرض مراحل الإنتاج من GitHub Actions في `podcast_production`.
- fallback: تعطل التحليلات لا يعطل التصفح؛ تعطل فهرس زمني لا يختلق توقيتًا؛ فشل مصدر من بحث الموسوعة لا يمنع بقية المصادر.
