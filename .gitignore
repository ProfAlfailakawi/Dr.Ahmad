# اعتماديات ومخرجات
node_modules
dist

# أسرار — لا تُرفع أبداً
.env
.env.local
.env.*.local
sa.json
*service-account*.json
*firebase-adminsdk*.json
scripts/cloud-radar.gs

# أصول مكرّرة (المصدر المعتمد للبناء هو مجلدات الجذر audio/ covers/ files/)
public/audio
public/covers
public/files

# بقايا بناء قديمة في الجذر
/assets/*.js
/assets/.aistudio

# نظام
.DS_Store
whatsapp-agent/node_modules/
whatsapp-agent/package-lock.json
whatsapp-agent/*.sqlite
whatsapp-agent/*.sqlite-*
whatsapp-agent/session/
whatsapp-agent/auth/
whatsapp-agent/logs/
Library/Application Support/DrAhmadWhatsAppAgent/
.whatsapp-agent/
*.log

# مخرجات بناء لا تُلتزم في الجذر (الناتج الرسمي dist/ فقط)
/about/ /admin/ /articles/ /atlas/ /contact/ /curated/ /cv/ /inbox/ /media/
/publications/ /questions/ /radar/ /research/ /search/ /upcoming/ /assets/ /og/
/*.html
!/index.html
/sitemap.xml
/feed.xml
.notify-state.json
.cron-secret.local
.podcast-state.json
.podcast-dialogue.lock
.podcast-bakeoff-public.backup/
podcast-audits/voice-bakeoff.private.json
podcast-rejected/
.podcast-tmp/
.audio-human-work/
.audio-last-known-good/
.audio-site-patches.json
audio-audits/quarantine/
audio-audits/**/*.mp3
ArabicPronunciationMemory.sqlite*
.private-memory/
PrivateBooks/
.firebase/hosting.ZGlzdA.cache

# الصوت الجديد لا يضاف إلى Git بعد الانتقال إلى R2
audio/**/*.mp3
audio/**/*.json
!audio/.gitkeep
public/audio/**/*.mp3
public/audio/**/*.json
podcast-finalist-retest-samples/
podcast-dialogue-samples/
podcast-voice-bakeoff-samples/
.audio-r2-plan.json
whatsapp-agent/.env

# نسخُ الطوارئ المحلية — فيها قاعدة بيانات الوكيل بمفاتيح جلسة واتساب
# وجهاتِ الاتصال. لا تُرفع أبداً: من ملك المفاتيح ربط جهازه بحساب الدكتور.
.codex-backups/
