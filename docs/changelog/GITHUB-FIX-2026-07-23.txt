إصلاح GitHub Actions — 2026-07-23

السبب الجذري:
مؤقت لقطة قاعدة البيانات في whatsapp-agent/agent.mjs كان يبدأ بعد 150 ثانية أثناء self-test.
إذا انتهى الاختبار وأُغلقت SQLite بينما المهمة غير المتزامنة تكمل عملها، كانت محاولة تسجيل الخطأ
تستخدم قاعدة مغلقة وتنتج ERR_INVALID_STATE ثم exit code 1.

الإصلاح:
1) عدم تشغيل مؤقتات التقرير والنسخ الاحتياطي في وضع mock/self-test.
2) حماية تسجيل فشل النسخة إذا كانت القاعدة قد أُغلقت أثناء الإيقاف.
3) إزالة نسخ .codex-backups وقواعد SQLite المحلية وأسماء النسخ المشوهة/الطويلة من حزمة الرفع.

إصدار Node.js 24.18.0 ليس سبب المشكلة، لذلك بقي كما هو.
