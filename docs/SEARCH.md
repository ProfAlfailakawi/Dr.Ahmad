# البحث والروابط العميقة

البحث العام والموسوعة يحفظان الحالة المهمة في query parameters. الموسوعة تدعم `q`, `tab`, `video`, `t`, `result` عند الحاجة. `pushState` للانتقال المهم و`replaceState` للتحديثات الثانوية. فتح رابط فيديو يجهزه عند الوقت ولا يشغله تلقائيًا بصوت.
